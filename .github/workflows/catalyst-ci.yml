name: Catalyst CI
on: [pull_request]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run placeholder tests
        run: |
          echo "✅ smoke-test: repo integrity OK"